export const PUBLIC_ROUTE = '/p';

export const PUBLIC_HOME = PUBLIC_ROUTE + '/home';

export const PUBLIC_RESERVATIONS = PUBLIC_ROUTE + '/reservations';

export const PUBLIC_REGISTRATION = PUBLIC_ROUTE + '/registration';

